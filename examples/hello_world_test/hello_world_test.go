package hello_world_test

import (
	"testing"
)

func TestHelloWorld(t *testing.T) {

	// import the base system plus some extra(s) on top
	// system := cbtest.ImportSystem(t, "../base_system", "./extra")

	// destroy the created system after we are done testing

}
